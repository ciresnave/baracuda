[package]
name = "baracuda-seam"
version = "0.0.1-alpha.68"
description = "Profile v1 kernel-seam handshake: the frozen SeamHello negotiation envelope (§3.1) + the baracuda_seam_hello() provider entry point Fuel reads to negotiate a seam profile + capabilities."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
keywords = ["cuda", "ffi", "interop", "dlpack"]
categories = ["api-bindings"]

[lints]
workspace = true
